<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--组件扫描器 @Controller -->
    <context:component-scan base-package="org.example.controller"></context:component-scan>

    <!--视图解析器-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--前缀：视图的文件路径-->
        <property name="prefix" value="/WEB-INF/view/"></property>
        <!--后缀：视图文件的扩展名-->
        <property name="suffix" value=".jsp"></property>
    </bean>

    <mvc:annotation-driven/>
    <!--注解驱动 做ajax的 因为这里用到的是注解做ajax 当然
    也可以解决和@RequestMapping有冲突的字段 比如下面这个
    就必须加入  <mvc:annotation-driven/> 来解决冲突问题-->

    <mvc:resources mapping="/static/**" location="/static/"/>
    <!--这段话会创建出一个对象 这个对象专门来处理静态资源文件、当遇到类似images.xx.png的
    静态资源文件的时候 就去static路径下面找  两个*代表static路径下的所有的文件-->



</beans>